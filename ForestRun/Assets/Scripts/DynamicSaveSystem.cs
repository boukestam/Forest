using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class DynamicSaveSystem : MonoBehaviour {

	// Use this for initialization
	void Start () {
        //TODO SAVE LEVEL UNLOCKED
        //PlayerPrefs.SetInt("Level" + levelNumber, 1);
        //PlayerPrefs.SetInt("Level" + levelNumber + "_score", score);
    }

    // Update is called once per frame
    void Update () {
		
	}
}
